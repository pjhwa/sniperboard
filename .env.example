# 로컬 환경에 맞게 복사 후 수정: cp .env.example .env
# Docker 환경: BACKEND_URL은 docker-compose.yml에서 자동 설정됩니다 (http://backend:8000).
# 로컬 개발(docker 없이 Next.js dev server 직접 실행 시)에만 아래를 설정하세요.
# BACKEND_URL=http://localhost:5001
